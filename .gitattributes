# These were tracked with Git LFS, which GitHub Pages does not resolve: the
# published site was served the pointer files instead of the images. They are
# ordinary blobs now, kept small by dp/optimize_images.py.
*.png binary
*.pdf binary
