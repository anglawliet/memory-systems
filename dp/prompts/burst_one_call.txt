BURST ONE RECURSIVE CALL (landscape, loop of choices)
==============================================================
Landscape 1536x1024. Explains exactly ONE invocation of
best(left_edge, right_edge).

SHAPE: a LOOP over which balloon is burst LAST inside the open range. Choosing last, not first, is what makes the two sides independent.
This one branches over a LOOP, not two fixed choices, so the page
fans into sample lanes and merges them all with one operator.
Generated by build_cards.py from specs.py.
==============================================================

================================================================================
PROMPT 1: One Recursive Call -> burst_one_call.png
================================================================================

Create a landscape hand-drawn educational infographic, 1536x1024.

STYLE: a bright, fun classroom poster for a 10 year old, in friendly hand-drawn
marker lettering.
- Page background: soft warm cream with a faint pale dotted grid, plus a few
  small confetti dots and doodle stars in the empty areas.
- Every major box has a SOLID SATURATED colored header bar with its title in
  bold WHITE lettering, a soft pastel tint filling the body, a thick rounded
  outline and a soft drop shadow.
- Thick COLORED arrows matching the block they point to, big friendly
  arrowheads.
- Two or three BIG cute cartoon doodles with smiling faces and rosy cheeks,
  tucked into the margins so they never overlap text.
- A yellow sticky note with a red pushpin for the example.
- All lettering large, dark and crisp on the pale fills. Never shrink text.

HOW THE DATA IS DRAWN, VERY IMPORTANT:
Never draw the input as a row of individually coloured cells, and never write a
row of index numbers under it. That is visual noise. Instead:
- The input is ONE long flat rounded bar in plain neutral GREY, empty, with
  nothing written along it. Keep the bar LONG. The whole effect depends on one
  small bright square sitting on a long empty bar.
- Exactly ONE square sticks out of that bar: the current element, filled BRIGHT
  PURPLE with its value in bold white, and its index written under it ONLY in
  the CURRENT CALL box.
- Any part already used or dropped is pale grey, hatched, and crossed out.
- Never highlight two squares on the same bar.

STEP BADGES: each step gets a big round numbered badge in the colour of that
step, on the top left corner of its block. The same numbers appear beside the
matching lines in the code panel.

TITLE across the top, very large, first words purple, rest black:
   "BURST BALLOONS  -  ONE CALL"

TOP LEFT: a yellow sticky note with a red pushpin and a small target doodle:
   "THE EXAMPLE"
   "balloons =   3   1   5   8"
   with the four values as small plain cells
   "bursting a balloon pays left neighbour x it x right neighbour"
   "GOAL: burst them all in the order that pays the most"
   "best order pays  ->  answer 167"
   Then one small green line:
   "imagine a 1 glued to each end so the edges behave." 

TOP CENTER: box with a purple header bar reading "THE CALL ARRIVES".
   On its left, large purple lettering:
      "best( left_edge = 0 , right_edge = 5 )"
   under it, one small black line: "Returns the most coins from bursting everything strictly between."
   On its right:
      A long flat grey bar labelled "balloons", with a padded 1 drawn faintly at
      each end. A BRIGHT PURPLE square marks left_edge and a TEAL square marks
      right_edge, both labelled, and the whole span between them tinted pale
      yellow with the label "still to burst, and both edges SURVIVE".

TOP RIGHT: a box with a dark slate header bar reading "THE CODE". Very pale
grey body. Short lines, each numbered line carrying a small round coloured badge:
      "best( left_edge , right_edge ):"
   badge 1, blue:    "if right_edge - left_edge < 2: return 0"
   badge 2, blue:    "top = 0"
   badge 3, green:   "for last in range( left_edge+1 , right_edge ):"
                     "     pay  = nums[left_edge]*nums[last]*nums[right_edge]"
                     "     pay += best( left_edge , last )"
                     "     pay += best( last , right_edge )"
                     "     top  = max( top , pay )"
   badge 4, purple:  "return top" 

=== STEP 1, full width, blue, badge "1" ===

A wide band, solid BLUE header bar, white text: "STEP 1   IS THE RANGE EMPTY?".
Pale blue body, two short lines:
   "if  the two edges are neighbours   ->   return 0"
   "there is no balloon strictly between them, so nothing to burst"
On the right end, a small green arrow curving down labelled
   "not done, keep going"

=== STEP 2, full width, blue, badge "2" ===

A centered SOLID BLUE pill, bold white lettering:
   "WHICH BALLOON DO I BURST LAST?"
Under it, one small black line: "last, not first. That is the whole trick."

=== STEP 3, THE FAN OUT, badge "3" ===

From that pill, THREE thick coloured arrows fan downward into three narrow
lanes sitting side by side, each showing one option being tried. To the right
of the third lane, a large grey "..." with a small label "and so on for every
option", so it is clear the loop keeps going.

   LANE 1, green:
      A pill reading "BURST THE 3 LAST".
      Below it:
      The bar with the 3 highlighted gold and labelled "survives until the
      end", and everything either side tinted as two separate sub ranges.
      Then a purple-outlined box:
         "pay + best( left , here ) + best( here , right )"
         "=   1 x 3 x 1  plus the two sides"

   LANE 2, blue:
      A pill reading "BURST THE 5 LAST".
      Below it:
      The bar with the 5 highlighted gold, and two clearly separate tinted
      spans either side of it, labelled "left part" and "right part".
      Then a purple-outlined box:
         "pay + best( left , here ) + best( here , right )"
         "=   1 x 5 x 1  plus the two sides"

   LANE 3, red:
      A pill reading "BURST THE 8 LAST".
      Below it:
      The bar with the 8 highlighted gold and the two sides tinted
      separately.
      Then a purple-outlined box:
         "pay + best( left , here ) + best( here , right )"
         "=   1 x 8 x 1  plus the two sides"

=== STEP 4, THE MERGE, full width at the bottom, purple, badge "4" ===

Every lane sends a thick arrow down into ONE wide box at the bottom centre.
Solid PURPLE header bar, white text: "KEEP THE RICHEST ORDER".
Pale purple body, with a shiny gold trophy doodle beside it. Large lettering:
   "return  max( every balloon tried as the last one )"
Under it, one short black line:
   "the two sides never interfere, because the last balloon is still standing"

=== CORNER BOX, no arrow touching it ===

A small box with an AMBER header bar reading "WHY LAST AND NOT FIRST":
   "If I burst one FIRST, its neighbours change and the two"
   "halves affect each other, so they cannot be solved apart."
   "Bursting it LAST keeps both edges alive, which cuts the"
   "range into two problems that never touch." 

COMPOSITION: one call at the top, then a wide fan of sample options, then every
lane funnelling back into a single answer. The "..." must make clear this is a
loop, not a fixed set of branches. Airy and colorful, every word spelled
correctly.
