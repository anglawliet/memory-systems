DISTINCTSUBSEQ ONE RECURSIVE CALL (landscape)
==============================================================
Landscape 1536x1024. Explains exactly ONE invocation of
count(source_index, target_index).

SHAPE: if / else, but the match branch is unusual: even when the letters match you must ALSO try skipping, so that branch adds two calls together.
Generated by build_cards.py from specs.py.
See MASTER_ONE_CALL_DIAGRAM_PROMPT.md for the rules behind this layout.
==============================================================

================================================================================
PROMPT 1: One Recursive Call -> distinctsubseq_one_call.png
================================================================================

Create a landscape hand-drawn educational infographic, 1536x1024.

STYLE: a bright, fun classroom poster for a 10 year old, in friendly hand-drawn
marker lettering.
- Page background: soft warm cream with a faint pale dotted grid, plus a few
  small confetti dots and doodle stars in the empty areas.
- Every major box has a SOLID SATURATED colored header bar with its title in
  bold WHITE lettering, a soft pastel tint filling the body, a thick rounded
  outline and a soft drop shadow.
- Thick COLORED arrows matching the block they point to, big friendly
  arrowheads.
- Two or three BIG cute cartoon doodles with smiling faces and rosy cheeks,
  tucked into the margins so they never overlap text.
- A yellow sticky note with a red pushpin for the example.
- All lettering large, dark and crisp on the pale fills. Never shrink text.

HOW THE DATA IS DRAWN, VERY IMPORTANT:
Never draw the input as a row of individually coloured cells, and never write a
row of index numbers under it. That is visual noise. Instead:
- The input is ONE long flat rounded bar in plain neutral GREY, empty, with
  nothing written along it. Keep the bar LONG. The whole effect depends on one
  small bright square sitting on a long empty bar.
- Exactly ONE square sticks out of that bar: the current element, filled BRIGHT
  PURPLE with its value in bold white, and its index written under it ONLY in
  the CURRENT CALL box.
- Any part already used or dropped is pale grey, hatched, and crossed out.
- Never highlight two squares on the same bar.

STEP BADGES: each step gets a big round numbered badge in the colour of that
step, on the top left corner of its block. The same numbers appear beside the
matching lines in the code panel.

TITLE across the top, very large, first words purple, rest black:
   "DISTINCT SUBSEQUENCES  -  ONE CALL"

TOP LEFT: a yellow sticky note with a red pushpin and a small target doodle:
   "THE EXAMPLE"
   "source =  r a b b b i t        target =  r a b b i t"
   with each string drawn as small plain cells
   "GOAL: count how many ways target appears inside source as a subsequence"
   "three different b choices  ->  answer 3"
   Then one small green line:
   "we are at the first letter of each." 

TOP CENTER: box with a purple header bar reading "THE CALL ARRIVES".
   On its left, large purple lettering:
      "count( source_index = 0 , target_index = 0 )"
   under it, one small black line: "Returns how many ways the rest of target hides in the rest of source."
   On its right:
      Two long flat grey bars, one above the other.
      Row "source": a BRIGHT PURPLE square showing "r" with "0" under it,
      labelled "source_index", then plain empty grey.
      Row "target": a BRIGHT PURPLE square showing "r" with "0" under it,
      labelled "target_index", then plain empty grey.

TOP RIGHT: a box with a dark slate header bar reading "THE CODE". Very pale
grey body. Short lines, each numbered line carrying a small round coloured badge:
      "count( source_index , target_index ):"
   badge 1, blue:    "if target_index == len(target): return 1"
                     "if source_index == len(source): return 0"
   badge 2, blue:    "source_letter = source[ source_index ]"
                     "target_letter = target[ target_index ]"
   badge 3, green:   "if they match:"
                     "     return use_it + skip_it"
   badge 4, red:     "return skip_it" 

A thick arrow leads down the centre from THE CALL ARRIVES into:

=== STEP 1, full width, blue ===

A wide band, solid BLUE header bar, white text: "STEP 1   IS THE TARGET FULLY MATCHED?".
Big round blue badge "1". Pale blue body, two short lines:
   "if  target_index == 6   ->   return 1"
   "the whole target was found, that is one complete way. Source running out returns 0."
On the right end, a small green arrow curving down labelled
   "not done, keep going"

=== STEP 2, full width, blue ===

A centered SOLID BLUE pill, bold white lettering, big round blue badge "2":
   "source_letter  ==  target_letter  ?"

=== THE SPLIT ===

From that pill, two thick coloured arrows fan out, one curving down LEFT and
one curving down RIGHT, into two columns side by side that run to the bottom
and NEVER rejoin. Green arrow to the left column, red arrow to the right
column. Between them, a long vertical DASHED grey line with a small grey pill
on it reading "only one branch runs".

=== LEFT COLUMN (green), badge "3" ===

Solid GREEN header bar, white text: "THEY MATCH   ->   TWO WAYS TO USE IT".
Pale green body:
   Two small stacked rows, each in its own thin purple-outlined box:
      "USE IT     count( source_index + 1 , target_index + 1 )  = count(1,1)"
         small line: "spend this source letter on the target"
      "SAVE IT    count( source_index + 1 , target_index )      = count(1,0)"
         small line: "keep looking for another copy later"
   A big green PLUS sign between the two boxes, large and bold.
   Then a purple-outlined box:
      "count( s+1 , t+1 )  +  count( s+1 , t )"
      "=   count( 1 , 1 ) + count( 1 , 0 )"
   Then one short black line: "both futures are counted, not compared"

=== RIGHT COLUMN (red), badge "4" ===

Solid RED header bar, white text: "THEY DIFFER   ->   ONLY ONE WAY".
Pale red body:
   The source bar redrawn with its purple square crossed out in red and the
   pointer moved on. The target bar is untouched, with a red label
   "target_index does not move".
   Then a purple-outlined box:
      "count( source_index + 1 , target_index )"
      "=   count( 1 , 0 )"
   Then one short black line: "this source letter is useless here, walk past it"

A thick green arrow leads down to a green return box at the bottom of the LEFT
column, dark green lettering:
   "return  count( s+1 , t+1 ) + count( s+1 , t )"

A thick red arrow leads down to a red return box at the bottom of the RIGHT
column, red lettering:
   "return  count( source_index + 1 , target_index )"

=== CORNER BOX, no arrow touching it ===

A small box with an AMBER header bar reading "WHY MATCHING STILL SKIPS":
   "A matching letter is not forced to be the one you use."
   "Another copy further along may start a different way,"
   "and we are counting ways, so both are added." 

COMPOSITION: the page reads top to bottom in the exact order the code runs.
Airy and colorful, calm grey bars with a single bright highlight drawing the eye
in each row. Every word spelled correctly.
