MIN COST CLIMBING STAIRS: Full Walkthrough
=============================================
The student already did the Climbing Stairs page.
Min Cost is the SECOND problem. Teach it fully from scratch:
question, choices (make this very intuitive), recursion, array, fill, code.

IMPORTANT VISUAL ACCURACY RULES:
  - The staircase has 3 steps numbered 0, 1, 2 and a TOP platform.
  - Each step has a TOLL SIGN posted on that step's surface.
  - Step 0's toll sign says "$10" and sits on step 0.
  - Step 1's toll sign says "$15" and sits on step 1.
  - Step 2's toll sign says "$20" and sits on step 2.
  - The TOP platform has a green flag (no toll).
  - "Pay the toll" means: you pay the cost WHEN YOU LEAVE that step,
    not when you arrive. Standing on a step is free. Leaving costs money.
  - Arrows in path diagrams go UP (from lower step to higher step).

Example: cost = [10, 15, 20]
  Answer: $15 (start at step 1, pay $15, climb 2 to top)

Descriptive names:
  cost_to[step]        not  dp[i]
  cost[step]           not  cost[i]
  from_one_below       not  dp[i-1] + cost[i-1]
  from_two_below       not  dp[i-2] + cost[i-2]

Images produced:
  Prompt 1 -> min_cost_1.png  (The Question + How to Find the Choices)
  Prompt 2 -> min_cost_2.png  (Recursive Code + Tree + Overlap)
  Prompt 3 -> min_cost_3.png  (The Array + Filling Every Cell + Code)


================================================================================
STYLE BIBLE (copy into every prompt)
================================================================================

PASTE THIS BLOCK AT THE TOP OF EVERY MIN COST PROMPT.

---BEGIN STYLE BIBLE---

IMAGE FORMAT: Tall portrait infographic (1024x1792).

TEXT RULES:
- ALL text must be HUGE. Section titles 60pt+, body text 32pt+, code 26pt+.
- Every element must be readable on a phone held vertically WITHOUT zooming.
- Maximum 15 words per explanation. Let the visuals carry the meaning.
- White space is OK. Don't cram.
- All text rendered in a clean sans-serif font (like Nunito or Poppins).
  NO chalk-style text, NO handwritten fonts, NO serif fonts.

CHARACTER (Maya):
- Young woman, mid-20s, athletic build, medium-brown skin (warm undertone).
- Hair: jet-black, pulled into a SHORT LOW PONYTAIL with a few loose strands
  framing her face. Never a bun, never loose, never long.
- Face: round cheeks, big expressive dark-brown eyes, thick eyebrows,
  small nose, confident smirk or grin.
- Outfit (EVERY image, no exceptions):
  * Fitted dark-olive-green cargo pants (slightly dusty at the knees).
  * Snug black tank top.
  * A brown leather CHALK BAG clipped to her right hip.
  * Fingerless black climbing gloves on both hands.
  * Dark-gray low-top sneakers.
  * A playful black headband on her forehead.
- Proportions: Pixar/Disney-style 3D render. Head slightly oversized
  (about 1/4 of body height). Big eyes. Smooth plastic-like skin.
  Soft ambient occlusion shadows. No photorealism.

ART STYLE:
- Soft 3D cartoon render (Pixar energy). NOT flat illustration,
  NOT photorealistic, NOT anime, NOT watercolor.
- Smooth rounded shapes, soft shadows, slight subsurface scattering on skin.
- Clean vector-like line quality on diagrams and text boxes.

COLOR PALETTE (use ONLY these colors):
- Background: solid warm cream (#FFF8F0). No textures, no gradients,
  no chalkboard, no paper grain. Just flat warm cream everywhere.
- Section cards: white (#FFFFFF) with a thin 2px border in warm gray (#D4C5B2)
  and 8px rounded corners. Subtle drop shadow (2px, 10% opacity black).
- Section number badges: solid circle, burnt orange (#E8722A), white number inside.
- Headers: dark charcoal (#2D2D2D), bold.
- Body text: dark gray (#4A4A4A).
- Code blocks: dark navy background (#1E293B), white monospace text,
  8px rounded corners.
- POSITIVE / chosen / cheaper: leaf green (#4CAF50).
- ALTERNATIVE / other path: warm orange (#FF9800).
- DANGER / wrong / expensive: red (#E53935).
- Highlights / key insight boxes: pale gold fill (#FFF3D4) with
  gold border (#F5C542), 8px rounded corners.
- Array cells: white fill, charcoal border, charcoal text.
- Base-case cells: pale gold fill (#FFF3D4), gold border (#F5C542).
- Current/highlighted cell: pale green fill (#E8F5E9), green border (#4CAF50).

STAIRCASE VISUAL (same in every image):
- A grand stone staircase going UP from left to right.
- 3 wide steps plus a TOP platform at the upper right.
- The FRONT FACE of each step shows the step number in large bold text:
  "0" on the lowest step, "1" on the middle step, "2" on the highest step.
- Each step has a small wooden TOLL SIGN posted ON that step's surface:
  The sign on step 0 reads "$10".
  The sign on step 1 reads "$15".
  The sign on step 2 reads "$20".
  IMPORTANT: Each toll sign must be visually ON or NEXT TO its own step.
  Do NOT cluster all signs at the bottom. Do NOT put them above the steps.
- The TOP platform has a small green flag (destination, no toll to pay here).
- Staircase uses warm sandstone/beige color (#D4B896).

LAYOUT:
- Sections stacked vertically, separated by 20px of cream background.
- Each section inside a white card with the styling described above.
- Section number in a burnt-orange circle badge at the top-left of each card.

---END STYLE BIBLE---


================================================================================
PROMPT 1: The Question + How to Find the Choices -> min_cost_1.png
================================================================================

---BEGIN STYLE BIBLE---
[Paste the full style bible from above here]
---END STYLE BIBLE---

Stack 3 sections vertically:


SECTION 1 (badge: "1"), "THE QUESTION":
Header: "MIN COST CLIMBING STAIRS"
Subtitle: "LeetCode #746"

Show the staircase (from style bible) with Maya standing at the bottom,
looking up at the steps. Each step has its toll sign clearly visible
ON that step.

Problem statement in a white card:
  "Each step has a toll. You pay the toll when you LEAVE that step.
   From any step, you can climb 1 or 2 steps up.
   You can start on step 0 or step 1 (free to start).
   Goal: reach the top with minimum total cost."

Show the example visually ON the staircase.
Draw two different colored paths with arrows going UP:

  PATH A (orange arrows, more expensive):
    Orange arrow from step 0 up to step 2 (skip step 1).
    Orange arrow from step 2 up to TOP.
    Label on step 0: "pay $10"
    Label on step 2: "pay $20"
    Total shown: "$10 + $20 = $30"
    Small red "X" or faded style.

  PATH B (green arrows, cheaper):
    Green arrow from step 1 up to TOP (skip step 2 entirely).
    Label on step 1: "pay $15"
    Total shown: "$15"
    Green check mark. Label: "CHEAPEST!"

Maya speech bubble:
  "Same staircase, but now each step has a price."


SECTION 2 (badge: "2"), "HOW DO I FIND THE CHOICES?":
Header: "A SIMPLE TRICK TO FIND CHOICES IN ANY DP PROBLEM"

This is the MOST IMPORTANT section. Make it very clear and intuitive.

Show a 3-step process in 3 small stacked cards:

STEP A (white card):
  Header: "PICK ANY STEP IN THE MIDDLE"
  Show the staircase. Circle step 2 with a dotted highlight.
  Maya pointing at step 2.
  "Don't think about the whole problem. Just pick ONE step."

STEP B (white card):
  Header: "ASK: HOW COULD I HAVE GOTTEN HERE?"
  Show step 2 zoomed in. Draw two BACKWARD arrows pointing DOWN
  from step 2:

  Green arrow pointing down to step 1:
    "I climbed 1 step from step 1."
    "I must have paid the $15 toll at step 1 to leave it."

  Orange arrow pointing down to step 0:
    "I climbed 2 steps from step 0."
    "I must have paid the $10 toll at step 0 to leave it."

  "These are the ONLY two ways to reach step 2. That's it."

STEP C (white card):
  Header: "THE FORMULA WRITES ITSELF"

  Show both paths side by side:

  Green path card:
    "cheapest to step 1" + "$15 toll"
    = cost_to[1] + cost[1]

  Orange path card:
    "cheapest to step 0" + "$10 toll"
    = cost_to[0] + cost[0]

  "Pick the cheaper one: min(green, orange)"

  Gold highlight box with the formula:
    cost_to[step] = min(
        cost_to[step - 1] + cost[step - 1],
        cost_to[step - 2] + cost[step - 2]
    )


SECTION 3 (badge: "3"), "THIS TRICK WORKS FOR EVERY DP PROBLEM":
Header: "THE RECIPE"

Show a numbered checklist in a gold box:

  1. Pick any step in the middle
  2. Ask "how could I have gotten here?"
  3. List the ways (these are your CHOICES)
  4. For each way, write what it costs
  5. Combine them (min for cheapest, max for most, + for count)
  6. That's your formula

Maya speech bubble:
  "I used the same trick for Climbing Stairs.
   The only difference: staircase used + (count all ways).
   Min Cost uses min() (pick the cheapest)."

Small comparison at the bottom:
  Left: "Stairs: ways[step] = ways[step-1] + ways[step-2]"
         "add ALL ways"
  Right: "Min Cost: cost_to[step] = min(path_A, path_B)"
         "pick CHEAPEST way"


================================================================================
PROMPT 2: Recursive Code + Tree + Overlap -> min_cost_2.png
================================================================================

---BEGIN STYLE BIBLE---
[Paste the full style bible from above here]
---END STYLE BIBLE---

Stack 3 sections vertically:


SECTION 1 (badge: "4"), "BASE CASES":
Header: "WHAT DO WE KNOW WITHOUT COMPUTING?"

Show two base case cards side by side:

  Gold card 1:
    "cheapest_to(0) = $0"
    "Start on step 0 for free. No toll to ARRIVE."
    Show Maya standing on step 0, no money leaving her hand.

  Gold card 2:
    "cheapest_to(1) = $0"
    "Start on step 1 for free. No toll to ARRIVE."
    Show Maya standing on step 1, no money leaving her hand.

Small note: "The toll is for LEAVING a step, not arriving.
You can stand on step 0 or step 1 for free."


SECTION 2 (badge: "5"), "THE RECURSIVE CODE":
Header: "TURN THE FORMULA INTO CODE"

Code in a dark navy code block:
  cost = [10, 15, 20]

  def cheapest_to(step):
      if step == 0: return 0
      if step == 1: return 0
      from_one_below = cheapest_to(step - 1) + cost[step - 1]
      from_two_below = cheapest_to(step - 2) + cost[step - 2]
      return min(from_one_below, from_two_below)

  cheapest_to(3)   # top = one past the last step

Annotations with colored labels:
  from_one_below -> green label: "came from 1 step back + its toll"
  from_two_below -> orange label: "came from 2 steps back + its toll"
  min(...)       -> "pick the cheaper path"
  cost[step - 1] -> "toll at the step I came FROM, not the step I'm AT"


SECTION 3 (badge: "6"), "THE TREE":
Header: "DRAW THE CALLS"
Subtitle: "cheapest_to(3)"

Tree diagram, drawn clearly with rounded boxes for each node:

              cheapest_to(3)
              /            \
    cheapest_to(2)    cheapest_to(1)
      /        \            |
cheapest_to(1)  cheapest_to(0)   = 0
     |              = 0          (base)
     = 0
   (base)

Left branches labeled: "from 1 below" in green.
Right branches labeled: "from 2 below" in orange.
Base nodes (0 and 1) have gold fill and text "= 0".

Circle BOTH appearances of "cheapest_to(1)" in yellow.
Draw a dashed line between them.
Label: "cheapest_to(1) asked TWICE. Same answer both times."

Bottom: show the computation results on the tree:
  cheapest_to(0) = 0
  cheapest_to(1) = 0
  cheapest_to(2) = min(0 + $15, 0 + $10) = $10
  cheapest_to(3) = min($10 + $20, 0 + $15) = $15

Maya speech bubble:
  "Overlap! Time to cache, then build the table."


================================================================================
PROMPT 3: The Array + Fill + Code -> min_cost_3.png
================================================================================

---BEGIN STYLE BIBLE---
[Paste the full style bible from above here]
---END STYLE BIBLE---

Stack 3 sections vertically:


SECTION 1 (badge: "7"), "THE ARRAY":
Header: "WHAT DOES EACH CELL MEAN?"

Show a 1D array with 4 cells. Labels ABOVE each cell:

  "cheapest      "cheapest      "cheapest      "cheapest
   to reach       to reach       to reach       to reach
   step 0"        step 1"        step 2"        TOP"

  [   ?   ]      [   ?   ]      [   ?   ]      [   ?   ]
   step 0         step 1         step 2         step 3 (top)

"cost_to[step] = cheapest total cost to reach this step"

Base cases in gold-fill cells:
  cost_to = [ 0 (gold) , 0 (gold) ,  ?  ,  ?  ]
            step 0      step 1      step 2  TOP

"Start at step 0 or step 1 for FREE. Base cases = $0."

Maya pointing at the last cell. Speech bubble:
  "cost_to[3] is the answer. Cheapest total to reach the top."


SECTION 2 (badge: "8"), "FILL IT STEP BY STEP":
Header: "FILL LEFT TO RIGHT"

Show cost = [10, 15, 20] and the staircase with toll signs
clearly visible ON each step (this is important for accuracy).

PANEL: cost_to[2]:
  Array: [ 0 (gold), 0 (gold), ? (green highlight), ? ]

  Show step 2 on the staircase with two arrows pointing BACKWARD:

  GREEN arrow from step 2 down to step 1:
    from_one_below = cost_to[1] + cost[1]
    = $0 + $15 = $15
    (started at step 1, paid the $15 toll to leave step 1)

  ORANGE arrow from step 2 down to step 0:
    from_two_below = cost_to[0] + cost[0]
    = $0 + $10 = $10
    (started at step 0, paid the $10 toll to leave step 0)

  min($15, $10) = $10. Orange path wins!
  cost_to[2] = 10

  Updated array: [ 0, 0, 10, ? ]

PANEL: cost_to[3] (TOP):
  Array: [ 0, 0, 10, ? (green highlight with gold border) ]

  Show the TOP platform with two arrows pointing BACKWARD:

  GREEN arrow from TOP down to step 2:
    from_one_below = cost_to[2] + cost[2]
    = $10 + $20 = $30
    (came from step 2, paid the $20 toll to leave step 2)

  ORANGE arrow from TOP down to step 1:
    from_two_below = cost_to[1] + cost[1]
    = $0 + $15 = $15
    (started at step 1, paid the $15 toll to leave step 1)

  min($30, $15) = $15. Orange path wins!
  cost_to[3] = 15

COMPLETED ARRAY (gold highlight box):
  cost_to = [ 0 ,  0 , 10 , 15 ]
             step 0  step 1  step 2  TOP

  "Answer: $15. Start at step 1, pay $15, climb 2 to top."

Show the winning path on the staircase in GREEN:
  step 1 -> top (single green arrow, label "$15").
Maya at the top, arms raised.


SECTION 3 (badge: "9"), "THE CODE":
Header: "THE SOLUTION"

Code in a dark navy code block:
  def min_cost_stairs(cost):
      steps_to_top = len(cost)
      cost_to = [0] * (steps_to_top + 1)

      cost_to[0] = 0
      cost_to[1] = 0

      for step in range(2, steps_to_top + 1):
          from_one_below = cost_to[step - 1] + cost[step - 1]
          from_two_below = cost_to[step - 2] + cost[step - 2]
          cost_to[step] = min(from_one_below, from_two_below)

      return cost_to[steps_to_top]

Annotations:
  cost_to[0] = 0, cost_to[1] = 0  ->  "Free starting points (base cases)"
  for step in range(2, ...)        ->  "Left to right, after base cases"
  cost[step - 1]                   ->  "The toll at the step you came FROM"
  min(...)                         ->  "Pick the cheaper path"
  return cost_to[steps_to_top]     ->  "Last cell = cheapest total cost"

Gold highlight box at the bottom:
  "CLIMBING STAIRS vs MIN COST:
   Stairs:    ways[step] = ways[step-1] + ways[step-2]       (ADD all ways)
   Min Cost:  cost_to[step] = min(path_A, path_B)            (PICK cheapest)
   Same array. Same left-to-right fill. Different operation."
