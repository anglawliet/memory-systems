SUBSETSUM ONE RECURSIVE CALL (landscape)
==============================================================
Landscape 1536x1024. Explains exactly ONE invocation of
can_make(item_index, amount_left).

SHAPE: the same take or skip choice as knapsack, but the answers are true and false, so the combine is OR instead of max.
Generated by build_cards.py from specs.py.
See MASTER_ONE_CALL_DIAGRAM_PROMPT.md for the rules behind this layout.
==============================================================

================================================================================
PROMPT 1: One Recursive Call -> subsetsum_one_call.png
================================================================================

Create a landscape hand-drawn educational infographic, 1536x1024.

STYLE: a bright, fun classroom poster for a 10 year old, in friendly hand-drawn
marker lettering.
- Page background: soft warm cream with a faint pale dotted grid, plus a few
  small confetti dots and doodle stars in the empty areas.
- Every major box has a SOLID SATURATED colored header bar with its title in
  bold WHITE lettering, a soft pastel tint filling the body, a thick rounded
  outline and a soft drop shadow.
- Thick COLORED arrows matching the block they point to, big friendly
  arrowheads.
- Two or three BIG cute cartoon doodles with smiling faces and rosy cheeks,
  tucked into the margins so they never overlap text.
- A yellow sticky note with a red pushpin for the example.
- All lettering large, dark and crisp on the pale fills. Never shrink text.

HOW THE DATA IS DRAWN, VERY IMPORTANT:
Never draw the input as a row of individually coloured cells, and never write a
row of index numbers under it. That is visual noise. Instead:
- The input is ONE long flat rounded bar in plain neutral GREY, empty, with
  nothing written along it. Keep the bar LONG. The whole effect depends on one
  small bright square sitting on a long empty bar.
- Exactly ONE square sticks out of that bar: the current element, filled BRIGHT
  PURPLE with its value in bold white, and its index written under it ONLY in
  the CURRENT CALL box.
- Any part already used or dropped is pale grey, hatched, and crossed out.
- Never highlight two squares on the same bar.

STEP BADGES: each step gets a big round numbered badge in the colour of that
step, on the top left corner of its block. The same numbers appear beside the
matching lines in the code panel.

TITLE across the top, very large, first words purple, rest black:
   "SUBSET SUM  -  ONE CALL"

TOP LEFT: a yellow sticky note with a red pushpin and a small target doodle:
   "THE EXAMPLE"
   "numbers =   3   34   4   12"
   with the four values as small plain cells
   "target = 7"
   "GOAL: can any subset add up to exactly 7?"
   "3 and 4  ->  answer yes"
   Then one small green line:
   "we are at the 3, still needing all 7." 

TOP CENTER: box with a purple header bar reading "THE CALL ARRIVES".
   On its left, large purple lettering:
      "can_make( item_index = 0 , amount_left = 7 )"
   under it, one small black line: "Returns true if some subset from here hits the target exactly."
   On its right:
      A long flat grey bar labelled "numbers", with a BRIGHT PURPLE square at
      index 0 showing "3", labelled "item_index".
      Beside the bar, a big TEAL chip reading "amount_left = 7", tiny label
      "how much is still owed".

TOP RIGHT: a box with a dark slate header bar reading "THE CODE". Very pale
grey body. Short lines, each numbered line carrying a small round coloured badge:
      "can_make( item_index , amount_left ):"
   badge 1, blue:    "if amount_left == 0: return True"
                     "if item_index == len(numbers): return False"
   badge 2, blue:    "number = numbers[ item_index ]"
   badge 3, green:   "take = False"
                     "if number <= amount_left:"
                     "     take = can_make( item_index+1, amount_left-number )"
   badge 4, red:     "skip = can_make( item_index + 1 , amount_left )"
   badge 5, purple:  "return take or skip" 

A thick arrow leads down the centre from THE CALL ARRIVES into:

=== STEP 1, full width, blue ===

A wide band, solid BLUE header bar, white text: "STEP 1   IS THE TARGET ALREADY MET?".
Big round blue badge "1". Pale blue body, two short lines:
   "if  amount_left == 0   ->   return True"
   "nothing left to pay means this subset worked. Running out of numbers is False."
On the right end, a small green arrow curving down labelled
   "not done, keep going"

=== STEP 2, full width, blue ===

A centered SOLID BLUE pill, bold white lettering, big round blue badge "2":
   "USE THIS NUMBER, OR PASS ON IT?"

=== THE SPLIT ===

From that pill, two thick coloured arrows fan out, one curving down LEFT and
one curving down RIGHT, into two columns side by side. Green arrow to the left
column, red arrow to the right column. Between the columns, near the top, a
small PURPLE pill reading "both are explored". Do NOT draw a dashed divider,
and do NOT write "only one branch runs".

=== LEFT COLUMN (green), badge "3" ===

Solid GREEN header bar, white text: "USE IT".
Pale green body:
   A small blue gate pill: "does it fit ?   3 <= 7   YES" with a green check.
   Then the bar redrawn with the purple square filled solid green and ticked,
   and the teal chip now reading "amount_left = 4" with a green arrow showing
   7 shrinking to 4.
   Then a purple-outlined box:
      "can_make( item_index + 1 , amount_left - number )"
      "=   can_make( 1 , 4 )"
   Then one short black line: "the debt shrinks by this number"

=== RIGHT COLUMN (red), badge "4" ===

Solid RED header bar, white text: "PASS ON IT".
Pale red body:
   The bar redrawn with the purple square pale grey and crossed out in red,
   and the teal chip still reading "amount_left = 7", unchanged.
   Then a purple-outlined box:
      "can_make( item_index + 1 , amount_left )"
      "=   can_make( 1 , 7 )"
   Then one short black line: "the debt is untouched"

=== THE MERGE, full width at the bottom, purple ===

One thick GREEN arrow from the left column and one thick RED arrow from the
right column BOTH enter the same wide box at the bottom centre. This shared box
is the point: unlike an if / else, both choices really happen and are compared.
Solid PURPLE header bar, white text: "ONE SUCCESS IS ENOUGH".
Pale purple body, three lines of large lettering, gold trophy doodle beside:
   "USE  =  can_make( 1 , 4 )"      written in green
   "PASS  =  can_make( 1 , 7 )"      written in red
   "return  take  or  skip"        in purple, with the operator biggest
Under them, one short black line:
   "we only need one subset to work, not the best one"

=== CORNER BOX, no arrow touching it ===

A small box with an AMBER header bar reading "SAME CHOICE, DIFFERENT COMBINE":
   "Knapsack asked for the most value and used max."
   "This one only asks whether it is possible,"
   "so the very same two branches join with OR." 

COMPOSITION: the page reads top to bottom in the exact order the code runs.
Airy and colorful, calm grey bars with a single bright highlight drawing the eye
in each row. Every word spelled correctly.
